# Reports: shipped outputs

The outputs layer. Shipped audits and recommendation sets, one folder per pass (`reports/<yyyy-mm-audit>/`). Each folder is the record of what was found and what was recommended at that point in time.

Findings cite live data or dated snapshots from `../intake/`. A report is a locked artifact — the working analysis lives in the production chat until it ships here.
